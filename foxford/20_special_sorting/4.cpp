#include <bits/stdc++.h>
using namespace std;

int n, m;
vector<int> a;
vector<int> b;

int main()
{
    cin >> n;
    a.resize(n);
    for (auto &i : a)
    {
        cin >> i;
    }

    cin >> m;
    b.resize(m);
    for (auto &i : b)
    {
        cin >> i;
    }

    return 0;
}